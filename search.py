from typing import List


def binary_search(target: float, numbers: List[float]) -> float:
    """
    Applies binary search on the given list.

    Args:
        target (float): target for search
        numbers (List[float]): List of numbers

    Returns:
        float: Search result
    """
    result: float = None
    ...
    return result
